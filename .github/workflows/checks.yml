name: checks

on:
  push:
    branches: [main, test]
  pull_request:
    branches: [main]

# The workflow only reads the code. Anything wider would hand a compromised step
# push access — the same thing VS-CI-004 flags in other people's pipelines.
permissions:
  contents: read

concurrency:
  group: checks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      # Third-party actions are pinned to a commit, not a tag: a tag can be
      # moved under us, which is exactly what VS-CI-002 reports.
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck
        run: npx tsc --noEmit
      - name: Contrast audit (WCAG + colour-vision)
        run: npm run audit:contrast
      - name: Localization completeness
        run: npm run audit:i18n
      # SARIF goes straight into GitHub code scanning, which either accepts it
      # or rejects the upload — nothing in review reads it.
      - name: SARIF export shape
        run: npm run audit:sarif
      # The dashboard's headline number — a broken score lies silently.
      - name: Security score maths
        run: npm run audit:score
      # 1C loads this XML unattended — malformed or unescaped fails silently.
      - name: 1C export shape
        run: npm run audit:xml1c
      # Excel/LibreOffice silently refuse a malformed SpreadsheetML workbook.
      - name: Excel export shape
        run: npm run audit:excel
      # A misspelled design token drops the whole declaration without a word.
      - name: CSS token references
        run: npm run audit:cssvars
      # A setting nobody reads still saves, still draws a switch, still animates.
      # Nine of them shipped that way before this check existed.
      - name: Settings are wired up
        run: npm run audit:settings
      # `tsc --noEmit` says the types hold; it says nothing about whether the
      # bundle builds. Rollup's tree-shaking, env inlining and asset handling
      # only happen here.
      - name: Production build
        run: npm run build

  # A scanner that reports vulnerable dependencies in other people's projects
  # should know about its own. Both tools read the lockfiles, so this needs
  # neither a build nor the Tauri system libraries and stays a fast job.
  deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: npm audit
        run: npm audit --audit-level=high
      # cargo-audit is pinned: the advisory database now carries CVSS 4.0 entries
      # (RUSTSEC-2026-0041), and an older audit binary fails to parse the whole
      # database rather than skipping the entry. Letting the tool float meant the
      # job broke on someone else's advisory being published.
      - uses: taiki-e/install-action@0758d235715de2f3551eacc980d9ae8fce9342c3 # v2.87.3
        with:
          tool: cargo-audit@0.22.2
      # Fails on advisories only. The unmaintained/unsound warnings today are
      # GTK3 bindings Tauri pulls in for its Linux backend — not code that ships
      # in the Windows build, and not ours to fix.
      - name: cargo audit
        working-directory: src-tauri
        run: cargo audit

  backend:
    # Windows is what this ships on, and the code knows it: PATHEXT resolution
    # for .cmd shims, and parser fixtures captured from real tools on Windows
    # whose path normalisation only makes sense there. Testing on Linux would
    # mean testing something other than what users run.
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri
      - name: Tests
        working-directory: src-tauri
        run: cargo test --lib
      - name: Clippy
        working-directory: src-tauri
        run: cargo clippy --lib -- -D warnings
